import { CustomNgTimesDirective } from './custom-ng-times.directive';

describe('CustomNgTimesDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomNgTimesDirective();
    expect(directive).toBeTruthy();
  });
});
